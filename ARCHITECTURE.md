# Architecture

This document will describe SOLoRa's components, responsibilities, data flow, and deployment model.

Record architectural decisions alongside the implementation changes that introduce them.
