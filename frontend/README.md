# Frontend

The Phase 1 interface will use server-rendered HTML with minimal progressive enhancement. Keep templates, static CSS, and browser-side behavior here once the first local forum workflow is designed. No frontend product code belongs in Phase 0.
