# Frontend

This directory contains the React, TypeScript, and Vite client. Phase 0 exposes only a static development-foundation screen and its test; no forum workflow or backend integration is implemented yet.

Use `pnpm dev`, `pnpm test`, `pnpm lint`, `pnpm typecheck`, and `pnpm build` from this directory, or the corresponding root `make` commands.
